# ─────────────────────────────────────────
# SonarCloud — Configuración del proyecto
# laikaevents (React + FastAPI + MongoDB)
# ─────────────────────────────────────────

sonar.projectKey=KmsBismarck003_laikaevents
sonar.organization=kmsbismarck003

sonar.projectName=laikaevents
sonar.projectVersion=1.0

# ── Fuentes a analizar ──
# Ajusta estas rutas según tu estructura de carpetas
sonar.sources=.

# ── Exclusiones (no analizar) ──
sonar.exclusions=\
  node_modules/**,\
  **/node_modules/**,\
  **/__pycache__/**,\
  **/*.pyc,\
  **/migrations/**,\
  **/venv/**,\
  **/.venv/**,\
  **/dist/**,\
  **/build/**,\
  **/*.test.js,\
  **/*.spec.js,\
  **/coverage/**

# ── Cobertura de tests (opcional, agregar después) ──
# sonar.javascript.lcov.reportPaths=coverage/lcov.info
# sonar.python.coverage.reportPaths=coverage.xml

# ── Encoding ──
sonar.sourceEncoding=UTF-8
